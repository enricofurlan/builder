import { DynamicChildrenDirective } from './dynamic-children.directive';

describe('DynamicChildrenDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicChildrenDirective();
    expect(directive).toBeTruthy();
  });
});
