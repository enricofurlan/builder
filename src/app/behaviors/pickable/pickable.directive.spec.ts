import { PickableDirective } from './pickable.directive';

describe('PickableDirective', () => {
  it('should create an instance', () => {
    const directive = new PickableDirective();
    expect(directive).toBeTruthy();
  });
});
